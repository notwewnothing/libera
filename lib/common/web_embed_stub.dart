import 'package:flutter/widgets.dart';

/// Non-web stub: there is no DOM iframe off the web, so this is never shown
/// (the embed player only routes here when `EmbedMode.iframe`, i.e. on web).
Widget buildWebIframe(String url) => const SizedBox.shrink();
